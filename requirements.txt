# Notebooks 04 and 05 (counterfactual + forecast). The SQL/DBCODE notebooks
# need nothing beyond a SQLite client.
pandas>=2.0
matplotlib>=3.7
statsmodels>=0.14
numpy>=1.24
# To run the .ipynb files headlessly:
#   pip install nbconvert
#   jupyter nbconvert --to notebook --execute --inplace notebooks/04_counterfactual.ipynb
